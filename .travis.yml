sudo: required
services:
    - docker

brfore_install:
    - docker build -t pidixit/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true pidixit/docker-react npm run test -- --coverage
